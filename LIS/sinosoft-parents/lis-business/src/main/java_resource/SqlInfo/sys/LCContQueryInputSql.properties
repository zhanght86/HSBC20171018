LCContQuerySql1 =select a.PolNo,   (select distinct RiskCode  from LMRisk   where 1 = 1  and PolNo = a.PolNo  and RiskCode =   (select RiskCode      from LCPol     where PolNo = a.PolNo)) as RiskCode,   (select distinct RiskShortName  from LMRisk   where 1 = 1  and PolNo = a.PolNo  and RiskCode =   (select RiskCode      from LCPol     where PolNo = a.PolNo)),   (select CodeName  from LDCode   where 1 = 1  and CodeType = ''contstatetype''  and Code = a.StateType),   (select CodeName  from LDCode   where 1 = 1  and trim(CodeType) = lower(concat(concat(''cont'' , trim(a.StateType)) , ''state''))  and Code = a.State),   (select CodeName  from LDCode   where 1 = 1  and trim(CodeType) = lower(concat(concat(''cont'' , trim(a.StateType)) , ''reason''))  and Code = a.StateReason), a.StartDate, a.EndDate, a.Remark   from LCContState a  where 1 = 1  {0} order by RiskCode, a.StateType, a.StartDate, a.MakeDate asc
LCContQuerySql1_0 =and a.ContNo = ''{0}''

